version: '3'

services:

  archiver:
    build:
      context: ../.
      target: indexer
      dockerfile: Dockerfile
      args:
        - BUILD_CONTEXT=archiver
    environment:
      - RPC_ENDPOINT
      - MONGO_URI
    restart: unless-stopped

  archive-query-node:
    build:
      context: ../.
      target: queryNode
      dockerfile: Dockerfile
      args:
        - BUILD_CONTEXT=archiver
    ports:
      - 4050:80
    restart: unless-stopped
