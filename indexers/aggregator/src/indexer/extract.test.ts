describe('extract block', () => {
  it('should return a list of nft transfers', async () => {
    process.env.NFT_GRAPH =
      'https://ethereum-nft-beta.graph.eng.litentry.io/graphql';
    // const data = await extract(1, 1);
    // console.log(data);
    // console.log('done');
  });
});
