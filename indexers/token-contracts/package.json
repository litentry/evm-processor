{
  "name": "token-contracts",
  "description": "Indexes ERC20, ERC721 & ERC1155 contracts",
  "version": "0.0.1",
  "main": "lib/index.js",
  "scripts": {
    "db:reset": "docker-compose down && rm -rf ./db && docker-compose up -d",
    "start": "ts-node src/index.ts",
    "build": "rm -rf lib && tsc",
    "start:prod": "yarn build && node lib/index",
    "typecheck": "yarn tsc --noEmit",
    "_postinstall": "yarn run build"
  },
  "dependencies": {
    "archive-utils": "0.0.1"
  }
}
