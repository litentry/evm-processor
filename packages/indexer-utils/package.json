{
  "name": "indexer-utils",
  "version": "0.0.1",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "typecheck": "yarn tsc --noEmit",
    "postinstall": "yarn run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "evm": "^0.2.0"
  }
}
