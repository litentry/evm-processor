{
  "name": "indexer-monitoring",
  "version": "0.0.1",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "typecheck": "yarn tsc --noEmit",
    "postinstall": "yarn run build",
    "watch": "tsc --watch"
  }
}
