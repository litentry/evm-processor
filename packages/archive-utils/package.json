{
  "name": "archive-utils",
  "version": "0.0.1",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "typecheck": "yarn tsc --noEmit",
    "postinstall": "yarn run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "colors": "^1.4.0",
    "evm": "^0.2.0",
    "mongoose": "^6.2.10",
    "web3": "^v3.0.0-rc.5"
  }
}
