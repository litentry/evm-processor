{
  "name": "archive-utils",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "typecheck": "yarn tsc --noEmit",
    "postinstall": "yarn run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "mongoose": "^6.2.10"
  }
}
